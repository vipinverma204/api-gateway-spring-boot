server:
  port: 9090

spring:
  application:
    name: API-Gateway
#  cloud:
#    gateway:
#      routes:
#        - id: Employee-Onboarding
#          uri: lb://Employee-Onboarding
#          predicates:
#            - Path=/employee/onboarding/**

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost